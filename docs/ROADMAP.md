# Roadmap

## v0.1 (MVP)
- Menu bar app + settings
- Accessibility checks + guidance
- Button remap (keyboard shortcut actions)
- Basic smooth scrolling modes

## v0.2
- Per-app profiles
- Import/export settings JSON
- Better scroll physics tuning

## v0.3
- Per-device profiles (identify HID device)
- Horizontal scrolling (tilt wheel) support if available

## v1.0
- Gesture language (click+drag) like trackpad
- Onboarding tutorial
- Distribution hardening (signing/notarization, updates)
